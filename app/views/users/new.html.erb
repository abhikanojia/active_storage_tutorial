<h1>Create new user</h1>
<%= form_with model: User.new do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :avatar %>
  <%= f.file_field :avatar %>

  <%= f.submit "Submit" %>
<% end %>
